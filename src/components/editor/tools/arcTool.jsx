export class ArcTool {
  handleMouseDown() {
    return null;
  }

  handleMouseMove(_event, _worldPoint, _context, session) {
    return session ?? null;
  }

  handleMouseUp() {
    return null;
  }

  handleClick() {
    // Arc generation is driven by the inspector controls.
  }
}
